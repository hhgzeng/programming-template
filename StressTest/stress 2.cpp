#include <bits/stdc++.h>

using i64 = long long;

void solve() {
    // input your code
    
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    freopen("in.txt", "r", stdin);
    freopen("bad.txt", "w", stdout);

    int t = 1;
    while (t--) {
    	solve();
    }

    return 0;
}
